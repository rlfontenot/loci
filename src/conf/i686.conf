# linux specific configuration options
INSTALL_DIR=/usr/local

# Comment out these lines if you do not wish to use pthreads
THREADS = -DPTHREADS
THREAD_LIB = -lpthread

MACHINE_SPECIFIC = -DLINUX -DHAVE_FENWM -I/usr/local/include $(THREADS)
MAKE = make  
BASE_LIBS = -lhdf5CC /usr/local/lib/libhdf5.a -lz -lm -lfenv #(THREAD_LIB)


